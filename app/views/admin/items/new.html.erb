<%= form_for [@category,@category.items.build], url: admin_category_item_index_path(@category) do |item| %>
  <%= item.label :Name %>
  <%= item.text_field :name %>
  <%= item.submit 'Create Item' %>
<% end %>
